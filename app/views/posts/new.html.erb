<h1>New Post</h1>

# <%= render 'form', post: @post %>

# <%= link_to 'Back', posts_path %>

    
<%= form_for @post do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.text_field :description %>
    <%= f.submit %>
<% end %>